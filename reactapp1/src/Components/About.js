import React from 'react';

const About = (props) => (
  <div className="main-content">
    <h2>{ props.title }</h2>
    <p>This is a beginning App start learning how to use React and APIs.   The user can navigate to the 'Home', 'About', 'API', 'And more! section.</p>
  </div>
);

export default About;